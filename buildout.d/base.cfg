[buildout]
extends =
    versions.cfg
extensions =
    buildout-versions
versions = versions
develop = .

parts =
    mx-base
    bpython
    django
    fab

eggs =
    Fabric
    intranet

[django]
recipe = djangorecipe
eggs = ${buildout:eggs}
project = intranet
projectegg = intranet
settings = settings
test =
    org
    www

[bpython]
recipe = zc.recipe.egg:scripts
eggs =
     ${buildout:eggs}
scripts = bpython

[fab]
recipe = zc.recipe.egg:scripts
eggs =
     ${buildout:eggs}
scripts = fab

[mx-base]
recipe = collective.recipe.mxbase
