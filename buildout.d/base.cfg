[buildout]
extends =
    versions.cfg
extensions = buildout.dumppickedversions
versions = versions
develop = .

parts =
    mx-base
    bpython
    django

eggs =
    Fabric
    intranet

[django]
recipe = djangorecipe
eggs = ${buildout:eggs}
project = intranet
projectegg = intranet
settings = settings
test =
    org
    www

[bpython]
recipe = zc.recipe.egg:scripts
eggs =
     ${buildout:eggs}
scripts = bpython

[mx-base]
recipe = collective.recipe.mxbase
